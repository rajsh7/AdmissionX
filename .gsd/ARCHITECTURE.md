# Architecture

> Auto-generated by /map on 2026-03-19

## Overview

AdmissionX is an educational platform matching students with colleges, courses, and careers. It uses a modern Next.js App Router architecture with Server Components and a custom JWT-based authentication system. The application handles three distinct user types: Students, Colleges, and Administrators.

```
┌─────────────────────────────────────────┐
│              Next.js App Router         │
│         (Pages, API Routes, Layouts)    │
├─────────────────────────────────────────┤
│         [Business Logic Layer]          │
│     (Custom JWT Auth, Emailing, Search) │
├─────────────────────────────────────────┤
│            [Data Layer]                 │
│         (MySQL Pool singleton in db.ts) │
└─────────────────────────────────────────┘
```

## Components

### Core Application (app/)
- **Purpose:** Next.js App Router containing all pages, layouts, and API routes.
- **Location:** `app/`
- **Dependencies:** React, Framework routing, `lib/` modules.

### Shared UI Components (app/components/)
- **Purpose:** Reusable React components for the UI (e.g., HeroSection, CollegeCard, SearchFilters).
- **Location:** `app/components/`
- **Dependencies:** Tailwind CSS, Framer Motion, React.

### Data Layer (lib/db.ts)
- **Purpose:** Database connection management. Provides a global singleton MySQL pool to prevent connection exhaustion during development (HMR).
- **Location:** `lib/db.ts`
- **Dependencies:** `mysql2/promise`

### Authentication Service (lib/auth.ts)
- **Purpose:** Custom JWT authentication handling cookie-based sessions for students, colleges, and admins.
- **Location:** `lib/auth.ts`
- **Dependencies:** `jose`

### Email Service (lib/email.ts)
- **Purpose:** Handles transactional email sending (e.g., signup confirmation).
- **Location:** `lib/email.ts`
- **Dependencies:** `nodemailer`

## Data Flow

1. **Client Request**: User accesses a page or submits a form.
2. **Next.js Server / API**: The App router processes the request (often using React Server Components or direct API route endpoints).
3. **Authentication**: `lib/auth.ts` verifies the JWT cookie to authorize the specific role (Student/College/Admin).
4. **Database Query**: Queries are executed via the MySQL pool in `lib/db.ts`.
5. **Response**: Data is rendered via components in `app/components/` and returned to the client.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| MySQL Database | Database | Primary data store for users, colleges, reviews, etc. |
| SMTP Server | Email | Transactional emails handled via NodeMailer wrapper. |

## Technical Debt

- [ ] **Unorganized Root Directory**: Numerous `.mjs`, `.js`, `.script`, `.sql`, and `.txt` scripts clutter the project root. These should be consolidated into a `scripts/` or `db/` directory format.
- [ ] **Error Logging**: Extensive use of `console.error` inside try-catch blocks across Server Components. Lacks a unified structured logging mechanism or observability setup.
- [ ] **Database Schema Management**: Schema definition and updates appear to be managed by loose script files rather than a robust migration system (e.g., Prisma, Drizzle, or raw SQL migrations folder).

## Conventions

- **Naming:** PascalCase for React components, camelCase for functions and files in `lib/`.
- **Structure:** Next.js App Router patterns with colocated `components/` inside `app/` and business logic abstracted to `lib/`.
- **Testing:** End-to-end and database testing heavily rely on ad-hoc scripts in the root directory rather than a unified testing suite.
